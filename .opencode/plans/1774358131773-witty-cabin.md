# Plan: Use `mod` let-binding in both aerospace.nix and terminal.nix

Dendritic approach: repeat `mod = "alt"` in each file that needs it. No shared options, no layers.

## Changes

### `modules/terminal.nix`
- Add `mod = "alt";` to the existing `let` block (alongside `workspace`)
- Replace hardcoded `"alt-"` prefixes with `"${mod}-"` in the darwin aspect keybindings (lines 13-14)

### `modules/aerospace.nix`
- Fix inconsistency: `serviceBindings` (lines 130-145) hardcodes `alt-shift-{h,j,k,l}` instead of using `${mod}` — change to `"${mod}-shift-{h,j,k,l}"`
- `cmd-alt-h` on line 90 is intentionally different (disables macOS hide-others) — leave as-is

## Files
- `modules/terminal.nix` — 2-line change (add `mod`, use it in bindings)
- `modules/aerospace.nix` — 4-line change (serviceBindings `alt-shift-*` → `${mod}-shift-*`)

## Verification
- `nix build '.#darwinConfigurations.Alexs-MacBook-Air.system' --dry-run`
- Derivation hashes should be identical to current (output unchanged since mod is still "alt")
